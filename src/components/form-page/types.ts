interface IFormPageConfig {
  formConfig: {
    labelCol: { span?: number; offset?: number };
    wrapperCol: { span?: number; offset?: number };
  };

  formItemConfig: {};
}
export type { IFormPageConfig };
